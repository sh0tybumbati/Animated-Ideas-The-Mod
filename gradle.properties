# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true

# IntelliJ IDEA is not yet fully compatible with configuration cache, see: https://github.com/FabricMC/fabric-loom/issues/1349
org.gradle.configuration-cache=false

# Fabric Properties
# check these on https://fabricmc.net/develop
minecraft_version=26.1.2
loader_version=0.18.1 # Latest stable loader as of recent checks
loom_version=1.16-alpha.1 # Using a recent stable/alpha Loom version instead of snapshot
# Note: loom_version=1.16-SNAPSHOT is often the cause of such issues. Let's try a more recent, potentially stable one.

# Mod Properties
mod_version=1.0.0
maven_group=com.garrett.mod
mod_id=garrettmod

# Dependencies
fabric_api_version=0.148.0+26.1.2 # Latest compatible with MC 26.1.2
cloth_config_version=16.0.154 # Latest stable Cloth Config
mod_menu_version=18.0.0 # Latest stable Mod Menu
